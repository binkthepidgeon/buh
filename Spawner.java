package inkball;

/**
 * A spawner object with a position to spawn balls.
 */
public class Spawner extends GameObject {

    /**
     * Creates a Spawner at the given position.
     * @param x the x-coordinate
     * @param y the y-coordinate
     */
    public Spawner(int x, int y) {
        super(x, y);
    }
}